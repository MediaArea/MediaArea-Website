{{ form_start(form, {'method': 'post', 'action': path('user_guest_register'), 'attr': {'class': 'fos_user_registration_register recaptcha-form'}}) }}
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'registration.submit'|trans({}, 'FOSUserBundle') }}" id="btn-register-cb" />
    </div>
{{ form_end(form) }}
