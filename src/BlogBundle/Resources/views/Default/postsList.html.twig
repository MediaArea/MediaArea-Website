<div class="blog-post-list">
    {% for post in posts %}
        <h2><a href="{{ path('ma_blog_post', post.getUrlParams)}}">{{ post.title }}</a></h2>
        <p>{% if post.excerpt %}{{ post.excerpt }}{% else %}{{ post.htmlContent|striptags|truncate(400, true, '…')|raw}}{% endif %} <a href="{{ path('ma_blog_post', post.getUrlParams) }}">[continue]</a></p>
    {% endfor %}
</div>
